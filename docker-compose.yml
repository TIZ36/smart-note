services:
  local-embed:
    build:
      context: ./embedding
      dockerfile: Dockerfile
    ports:
      - "8009:8009"
    environment:
      - MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
    restart: unless-stopped
