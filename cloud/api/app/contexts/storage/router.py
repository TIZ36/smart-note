"""Storage context — HTTP transport (placeholder).

The actual storage routes still live in `app/routers/documents.py`.
This file exists so the layered import-linter contract can evaluate;
the full HTTP layer will land here in a follow-up refactor.
"""
