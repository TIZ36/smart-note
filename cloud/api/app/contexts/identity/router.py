"""Identity context — HTTP transport (placeholder).

Existing routes (`/v1/auth/*`, `/v1/devices/*`, `/v1/workspaces/*`)
still live in `app/routers/`. They migrate here once the identity
service.py absorbs the inline logic.
"""
