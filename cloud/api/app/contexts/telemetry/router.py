"""Telemetry context — HTTP transport (placeholder).

Existing routes (`/v1/usage`, `/v1/console/*`, `/v1/search/history`)
still live in `app/routers/`. They migrate here once the telemetry
service.py absorbs the inline aggregation.
"""
