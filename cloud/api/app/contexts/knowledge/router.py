"""Knowledge context — HTTP transport (placeholder).

Existing routes (search, retrieve, graph, wiki, tags) still live in
`app/routers/*.py`. They migrate here after the service+repository
layer absorbs the SQL currently inline in those routers.
"""
