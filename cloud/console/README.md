# cloud/console — Admin console (Next.js)

**Deferred to W7.** Placeholder so the monorepo layout is visible.

Minimal feature set at launch:
- Supabase OAuth login (GitHub + Google)
- List + create workspaces
- Issue / revoke API keys with scope selection
- Read-only memory list (filter by kind, search, pagination)

No billing UI in MVP; comes in v1.1 once quotas are enforced.
