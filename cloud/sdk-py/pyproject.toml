[project]
name = "smartnote-cloud"
version = "0.1.0"
description = "Python client for the SmartNote Cloud API — agent memory as a service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["smartnote_cloud*"]
